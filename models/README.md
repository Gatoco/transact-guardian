# This file is a placeholder to keep the models/ directory in git.
# In production, the actual model file (fraud_detection_model.pkl) should be
# volume-mounted or downloaded from a model registry.

# Download the dataset from:
# https://www.kaggle.com/datasets/dhanushnarayananr/credit-card-fraud

# To generate the model:
# 1. Download creditcard.csv to the project root
# 2. Run: python src/save_model.py
# 3. The model will be saved to models/fraud_detection_model.pkl